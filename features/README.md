# features/ — vertical slices

One folder per feature (sleep, todos, workouts, audio). Each owns its engine,
routes, panel markup+js, and tests. Editing a feature must touch only its folder.
See `docs/MIGRATION.md` and `../FEATURES.md`.
