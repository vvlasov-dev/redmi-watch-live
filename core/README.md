# core/ — thin shared plumbing

Fragile infra features register into but never edit: BT protocol, SQLite, the
HTTP router, shared state, and the watch-output arbiter. See `docs/MIGRATION.md`.
Empty until the backend move (do it on a morning with a full runway, never
before a sleep session).
